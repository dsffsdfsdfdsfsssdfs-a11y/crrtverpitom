import { NextResponse } from 'next/server'; import { isAdmin } from '@/lib/admin-auth'; import { readContent } from '@/lib/local-content'; export async function GET(){if(!await isAdmin())return NextResponse.json({error:'Войдите в редактор'},{status:401});try{return NextResponse.json(await readContent())}catch{return NextResponse.json({error:'Ошибка загрузки'},{status:500})}}