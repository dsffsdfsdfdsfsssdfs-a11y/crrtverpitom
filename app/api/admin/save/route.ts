import { NextResponse } from 'next/server'; import { isAdmin } from '@/lib/admin-auth'; import { saveContent } from '@/lib/github-content'; export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:'Войдите в редактор'},{status:401});try{await saveContent(await req.json());return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Ошибка сохранения'},{status:500})}}
